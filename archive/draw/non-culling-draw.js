// NON-CULLING (Draw all tiles)
// 0 (blocked) or 1 (moveable)
// for (let y = 0; y < mapH; y++) {
//   for (let x = 0; x < mapW; x++) {
//     switch (gameMap[y * mapW + x]) {
//       case 0:
//         game.fillStyle = "#999999";
//         break;
//       default:
//         game.fillStyle = "#eeeeee";
//         break;
//     }

//     game.fillRect(x * tileW, y * tileH, tileW, tileH);
//   }
// }
